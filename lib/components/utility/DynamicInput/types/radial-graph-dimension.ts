export interface RadialGraphDimension {
  /** 1–3 character abbreviation shown on the axis tip */
  abbr: string;
  /** Full name shown as a tooltip on hover of the abbreviation */
  label: string;
}
