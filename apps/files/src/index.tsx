import Browser from './apps/Browser';
import HomeWidget from './widgets/HomeWidget';

// Export apps for sub-app loading
export const apps = {
  Browser, // Main sub-app component
};

// Export widgets for widget loading
export const widgets = {
  HomeWidget,
};
